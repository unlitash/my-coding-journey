let todolist = ['learn arrays', 'make a to do list','do the to do list', 'use pop', 'use push','use shift', 'use unshift', 'use splice']

for (let list = 0; list < todolist.length; list++){
    console.log (`${list + 1}. ${todolist[list]}`)
}

// for (intialization ; condition ; expression) {}
// intialization: describes a variable
// condotion: describes the rules
// expression: does the process